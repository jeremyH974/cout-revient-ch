dist
dev-dist
coverage
# Générés par Stryker et par Vite (décision n° 147) : `.stryker-tmp` est une copie du projet,
# `.vite` le cache de pré-bundling sorti de `node_modules`, `reports` les rapports de mutation.
.stryker-tmp
.vite
reports
node_modules
playwright-report
test-results
blob-report
.lighthouseci
monitor-results
package-lock.json
tests/fixtures/**/*.csv
*.csv
tests/fixtures/**/*.json
# Pages officielles capturées telles quelles : les reformater changerait ce que l'analyseur lit,
# et le HTML de la Fed contient d'ailleurs un guillemet en trop que Prettier refuse d'analyser.
tests/fixtures/**/*.html
.claude
